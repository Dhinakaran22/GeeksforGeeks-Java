class Solution
{
    public long sum_of_square_oddNumbers(long n)
    {
        long no = n * 2;
        double a = 0;
        for (int i = 1;i<=no;i=i+2) {
            double c = Math.pow(i,2);
            a = a + c;
        }
        long b = (long) a;
        return b;
    }
}
